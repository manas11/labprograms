<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Bowlers"></activity>
        <activity android:name=".Batsman" />
        <activity android:name=".cricketerman" />
        <activity android:name=".fdef" />
        <activity android:name=".Fatt" />
        <activity android:name=".fman" />
        <activity android:name=".Footballers" />
        <activity
            android:name=".Choose"
            android:label="Choose Between Footballers and Cricketers">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>